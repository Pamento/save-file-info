{
  "name": "node-server-react",
  "version": "1.0.0",
  "description": "server nodejs serve react",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"nodemon server.js\" \"cd client && npm start\""
  },
  "author": "pamento",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "custom-env": "^1.0.0",
    "express": "^4.16.4"
  },
  "devDependencies": {
    "concurrently": "^4.1.0",
    "nodemon": "^1.18.10"
  }
}
